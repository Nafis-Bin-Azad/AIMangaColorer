# Manga Colorizer - Dependencies

# Core ML frameworks
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.28.0
transformers>=4.30.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# For manga-colorization-v2 (Fast engine)
gdown>=4.7.0
scikit-image>=0.21.0

# ControlNet preprocessing (for SD1.5 fallback)
controlnet-aux>=0.0.7

# Batch processing
tqdm>=4.65.0
natsort>=8.4.0

# Note: On Mac, install PyTorch with MPS support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
